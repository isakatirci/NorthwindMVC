/*************************************************************
** Class generated by CodeTrigger, Version 6.1.0.7
** This class was generated on 2.12.2018 01:22:24
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;
using System.Web.Http.Description;
using System.Threading.Tasks;
using Northwind.Mvc.SampleViewModels;
using Northwind.Business;
using Northwind.Business.Interfaces;
using Northwind.Business.Repository;
using Northwind.Business.Repository.Interfaces;

namespace Northwind.Mvc.SampleApiControllers
{
	public partial class CustomersController : ApiController
	{
		protected ICustomersRepository _iCustomersRepository;

            // GET: api/Customers
        public async Task<IHttpActionResult> GetCustomers(int page = 0, int pageSize = 5)
        {
            int totalCount = 0;

            IList<CustomersVm> listVm = new List<CustomersVm>();
            var result = Task.Factory.StartNew(() => {

                ICriteria criteria = new Criteria<BOCustomers>(CustomersRepository);
                totalCount = criteria.Count();

                IList<BOCustomers> listBOs = criteria
                    .Add(new OrderBy("id", OrderBy.OrderDirection.Ascending))
                    .Skip(page * pageSize)
                    .Take(pageSize)
                    .List<BOCustomers>();

                foreach (var bo in listBOs)
                    listVm.Add(new CustomersVm(bo));
                return listVm;
            });
            await result;

            return Ok(new { Data = result.Result, Paging = new { Total = totalCount, Limit = pageSize, CurrentPage = page, PageCount = (int) Math.Ceiling((double)totalCount / pageSize) } });
        }

        // GET: api/Customers/5
        [ResponseType(typeof(CustomersVm))]
        public async Task<IHttpActionResult> GetCustomers(string id)
        {
            var result = Task.Factory.StartNew(() => {

                BOCustomers bo = new Criteria<BOCustomers>(CustomersRepository)
                .Add(Expression.Eq("id", id))
                .SingleOrDefault<BOCustomers>();
                return bo == null ? null : new CustomersVm(bo);

            });
            await result;
            if (result.Result == null)
            {
                return NotFound();
            }

            return Ok(result.Result);
        }

        // PUT: api/Customers/5
        [ResponseType(typeof(void))]
        public async Task<IHttpActionResult> PutCustomers(string id, CustomersVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            if (id != vm.İd)
            {
                return BadRequest();
            }

            var result = Task.Factory.StartNew(() => {

                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { CustomersRepository }); 
                var bo = (BOCustomers)vm.BOCustomers(CustomersRepository);
                uow.Update(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return StatusCode(HttpStatusCode.NoContent);
        }

        // POST: api/Customers
        [ResponseType(typeof(CustomersVm))]
        public async Task<IHttpActionResult> PostCustomers(CustomersVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            var result = Task.Factory.StartNew(() =>
            {
                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { CustomersRepository }); 
                var bo = (BOCustomers)vm.BOCustomers(CustomersRepository);
                uow.Create(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                vm = new CustomersVm(bo);
                return true;
            });
            await result;
            return CreatedAtRoute("DefaultApi", new { id = vm.İd }, vm);
        }

        // DELETE: api/Customers/5
        [ResponseType(typeof(CustomersVm))]
        public async Task<IHttpActionResult> DeleteCustomers(string id)
        {
            var result = Task.Factory.StartNew(() =>
            {
                 IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { CustomersRepository }); 
                var bo = new BOCustomers();
                bo.Repository = CustomersRepository;
                bo.Init(id);
                
                uow.Delete(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return Ok(result.Result);
        }

        
        public ICustomersRepository CustomersRepository
        {
            get { return _iCustomersRepository ?? (_iCustomersRepository = RF.New().CustomersRepository); }
            set { _iCustomersRepository = value; }
        }
        

        protected override void Dispose(bool disposing)
        {
            if (disposing)
            {
            }
            base.Dispose(disposing);
        }
	}
}
