/*************************************************************
** Class generated by CodeTrigger, Version 6.1.0.7
** This class was generated on 2.12.2018 01:22:24
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;
using System.Web.Http.Description;
using System.Threading.Tasks;
using Northwind.Mvc.SampleViewModels;
using Northwind.Business;
using Northwind.Business.Interfaces;
using Northwind.Business.Repository;
using Northwind.Business.Repository.Interfaces;

namespace Northwind.Mvc.SampleApiControllers
{
	public partial class OrdersTaxStatusController : ApiController
	{
		protected IOrdersTaxStatusRepository _iOrdersTaxStatusRepository;

            // GET: api/OrdersTaxStatus
        public async Task<IHttpActionResult> GetOrdersTaxStatus(int page = 0, int pageSize = 5)
        {
            int totalCount = 0;

            IList<OrdersTaxStatusVm> listVm = new List<OrdersTaxStatusVm>();
            var result = Task.Factory.StartNew(() => {

                ICriteria criteria = new Criteria<BOOrdersTaxStatus>(OrdersTaxStatusRepository);
                totalCount = criteria.Count();

                IList<BOOrdersTaxStatus> listBOs = criteria
                    .Add(new OrderBy("id", OrderBy.OrderDirection.Ascending))
                    .Skip(page * pageSize)
                    .Take(pageSize)
                    .List<BOOrdersTaxStatus>();

                foreach (var bo in listBOs)
                    listVm.Add(new OrdersTaxStatusVm(bo));
                return listVm;
            });
            await result;

            return Ok(new { Data = result.Result, Paging = new { Total = totalCount, Limit = pageSize, CurrentPage = page, PageCount = (int) Math.Ceiling((double)totalCount / pageSize) } });
        }

        // GET: api/OrdersTaxStatus/5
        [ResponseType(typeof(OrdersTaxStatusVm))]
        public async Task<IHttpActionResult> GetOrdersTaxStatus(string id)
        {
            var result = Task.Factory.StartNew(() => {

                BOOrdersTaxStatus bo = new Criteria<BOOrdersTaxStatus>(OrdersTaxStatusRepository)
                .Add(Expression.Eq("id", id))
                .SingleOrDefault<BOOrdersTaxStatus>();
                return bo == null ? null : new OrdersTaxStatusVm(bo);

            });
            await result;
            if (result.Result == null)
            {
                return NotFound();
            }

            return Ok(result.Result);
        }

        // PUT: api/OrdersTaxStatus/5
        [ResponseType(typeof(void))]
        public async Task<IHttpActionResult> PutOrdersTaxStatus(string id, OrdersTaxStatusVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            if (id != vm.İd)
            {
                return BadRequest();
            }

            var result = Task.Factory.StartNew(() => {

                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrdersTaxStatusRepository }); 
                var bo = (BOOrdersTaxStatus)vm.BOOrdersTaxStatus(OrdersTaxStatusRepository);
                uow.Update(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return StatusCode(HttpStatusCode.NoContent);
        }

        // POST: api/OrdersTaxStatus
        [ResponseType(typeof(OrdersTaxStatusVm))]
        public async Task<IHttpActionResult> PostOrdersTaxStatus(OrdersTaxStatusVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            var result = Task.Factory.StartNew(() =>
            {
                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrdersTaxStatusRepository }); 
                var bo = (BOOrdersTaxStatus)vm.BOOrdersTaxStatus(OrdersTaxStatusRepository);
                uow.Create(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                vm = new OrdersTaxStatusVm(bo);
                return true;
            });
            await result;
            return CreatedAtRoute("DefaultApi", new { id = vm.İd }, vm);
        }

        // DELETE: api/OrdersTaxStatus/5
        [ResponseType(typeof(OrdersTaxStatusVm))]
        public async Task<IHttpActionResult> DeleteOrdersTaxStatus(string id)
        {
            var result = Task.Factory.StartNew(() =>
            {
                 IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrdersTaxStatusRepository }); 
                var bo = new BOOrdersTaxStatus();
                bo.Repository = OrdersTaxStatusRepository;
                bo.Init(id);
                
                uow.Delete(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return Ok(result.Result);
        }

        
        public IOrdersTaxStatusRepository OrdersTaxStatusRepository
        {
            get { return _iOrdersTaxStatusRepository ?? (_iOrdersTaxStatusRepository = RF.New().OrdersTaxStatusRepository); }
            set { _iOrdersTaxStatusRepository = value; }
        }
        

        protected override void Dispose(bool disposing)
        {
            if (disposing)
            {
            }
            base.Dispose(disposing);
        }
	}
}
