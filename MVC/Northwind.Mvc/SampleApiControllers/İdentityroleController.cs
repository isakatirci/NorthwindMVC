/*************************************************************
** Class generated by CodeTrigger, Version 6.1.0.7
** This class was generated on 2.12.2018 01:22:24
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;
using System.Web.Http.Description;
using System.Threading.Tasks;
using Northwind.Mvc.SampleViewModels;
using Northwind.Business;
using Northwind.Business.Interfaces;
using Northwind.Business.Repository;
using Northwind.Business.Repository.Interfaces;

namespace Northwind.Mvc.SampleApiControllers
{
	public partial class İdentityroleController : ApiController
	{
		protected IIdentityroleRepository _iİdentityroleRepository;

            // GET: api/İdentityrole
        public async Task<IHttpActionResult> Getİdentityrole(int page = 0, int pageSize = 5)
        {
            int totalCount = 0;

            IList<İdentityroleVm> listVm = new List<İdentityroleVm>();
            var result = Task.Factory.StartNew(() => {

                ICriteria criteria = new Criteria<BOIdentityrole>(İdentityroleRepository);
                totalCount = criteria.Count();

                IList<BOIdentityrole> listBOs = criteria
                    .Add(new OrderBy("Id", OrderBy.OrderDirection.Ascending))
                    .Skip(page * pageSize)
                    .Take(pageSize)
                    .List<BOIdentityrole>();

                foreach (var bo in listBOs)
                    listVm.Add(new İdentityroleVm(bo));
                return listVm;
            });
            await result;

            return Ok(new { Data = result.Result, Paging = new { Total = totalCount, Limit = pageSize, CurrentPage = page, PageCount = (int) Math.Ceiling((double)totalCount / pageSize) } });
        }

        // GET: api/İdentityrole/5
        [ResponseType(typeof(İdentityroleVm))]
        public async Task<IHttpActionResult> Getİdentityrole(string id)
        {
            var result = Task.Factory.StartNew(() => {

                BOIdentityrole bo = new Criteria<BOIdentityrole>(İdentityroleRepository)
                .Add(Expression.Eq("Id", id))
                .SingleOrDefault<BOIdentityrole>();
                return bo == null ? null : new İdentityroleVm(bo);

            });
            await result;
            if (result.Result == null)
            {
                return NotFound();
            }

            return Ok(result.Result);
        }

        // PUT: api/İdentityrole/5
        [ResponseType(typeof(void))]
        public async Task<IHttpActionResult> Putİdentityrole(string id, İdentityroleVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            if (id != vm.Id)
            {
                return BadRequest();
            }

            var result = Task.Factory.StartNew(() => {

                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { İdentityroleRepository }); 
                var bo = (BOIdentityrole)vm.BOİdentityrole(İdentityroleRepository);
                uow.Update(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return StatusCode(HttpStatusCode.NoContent);
        }

        // POST: api/İdentityrole
        [ResponseType(typeof(İdentityroleVm))]
        public async Task<IHttpActionResult> Postİdentityrole(İdentityroleVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            var result = Task.Factory.StartNew(() =>
            {
                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { İdentityroleRepository }); 
                var bo = (BOIdentityrole)vm.BOİdentityrole(İdentityroleRepository);
                uow.Create(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                vm = new İdentityroleVm(bo);
                return true;
            });
            await result;
            return CreatedAtRoute("DefaultApi", new { id = vm.Id }, vm);
        }

        // DELETE: api/İdentityrole/5
        [ResponseType(typeof(İdentityroleVm))]
        public async Task<IHttpActionResult> Deleteİdentityrole(string id)
        {
            var result = Task.Factory.StartNew(() =>
            {
                 IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { İdentityroleRepository }); 
                var bo = new BOIdentityrole();
                bo.Repository = İdentityroleRepository;
                bo.Init(id);
                
                uow.Delete(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return Ok(result.Result);
        }

        
        public IIdentityroleRepository İdentityroleRepository
        {
            get { return _iİdentityroleRepository ?? (_iİdentityroleRepository = RF.New().IdentityroleRepository); }
            set { _iİdentityroleRepository = value; }
        }
        

        protected override void Dispose(bool disposing)
        {
            if (disposing)
            {
            }
            base.Dispose(disposing);
        }
	}
}
