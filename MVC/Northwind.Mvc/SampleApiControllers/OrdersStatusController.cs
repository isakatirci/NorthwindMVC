/*************************************************************
** Class generated by CodeTrigger, Version 6.1.0.7
** This class was generated on 2.12.2018 01:22:24
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;
using System.Web.Http.Description;
using System.Threading.Tasks;
using Northwind.Mvc.SampleViewModels;
using Northwind.Business;
using Northwind.Business.Interfaces;
using Northwind.Business.Repository;
using Northwind.Business.Repository.Interfaces;

namespace Northwind.Mvc.SampleApiControllers
{
	public partial class OrdersStatusController : ApiController
	{
		protected IOrdersStatusRepository _iOrdersStatusRepository;

            // GET: api/OrdersStatus
        public async Task<IHttpActionResult> GetOrdersStatus(int page = 0, int pageSize = 5)
        {
            int totalCount = 0;

            IList<OrdersStatusVm> listVm = new List<OrdersStatusVm>();
            var result = Task.Factory.StartNew(() => {

                ICriteria criteria = new Criteria<BOOrdersStatus>(OrdersStatusRepository);
                totalCount = criteria.Count();

                IList<BOOrdersStatus> listBOs = criteria
                    .Add(new OrderBy("id", OrderBy.OrderDirection.Ascending))
                    .Skip(page * pageSize)
                    .Take(pageSize)
                    .List<BOOrdersStatus>();

                foreach (var bo in listBOs)
                    listVm.Add(new OrdersStatusVm(bo));
                return listVm;
            });
            await result;

            return Ok(new { Data = result.Result, Paging = new { Total = totalCount, Limit = pageSize, CurrentPage = page, PageCount = (int) Math.Ceiling((double)totalCount / pageSize) } });
        }

        // GET: api/OrdersStatus/5
        [ResponseType(typeof(OrdersStatusVm))]
        public async Task<IHttpActionResult> GetOrdersStatus(string id)
        {
            var result = Task.Factory.StartNew(() => {

                BOOrdersStatus bo = new Criteria<BOOrdersStatus>(OrdersStatusRepository)
                .Add(Expression.Eq("id", id))
                .SingleOrDefault<BOOrdersStatus>();
                return bo == null ? null : new OrdersStatusVm(bo);

            });
            await result;
            if (result.Result == null)
            {
                return NotFound();
            }

            return Ok(result.Result);
        }

        // PUT: api/OrdersStatus/5
        [ResponseType(typeof(void))]
        public async Task<IHttpActionResult> PutOrdersStatus(string id, OrdersStatusVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            if (id != vm.İd)
            {
                return BadRequest();
            }

            var result = Task.Factory.StartNew(() => {

                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrdersStatusRepository }); 
                var bo = (BOOrdersStatus)vm.BOOrdersStatus(OrdersStatusRepository);
                uow.Update(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return StatusCode(HttpStatusCode.NoContent);
        }

        // POST: api/OrdersStatus
        [ResponseType(typeof(OrdersStatusVm))]
        public async Task<IHttpActionResult> PostOrdersStatus(OrdersStatusVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            var result = Task.Factory.StartNew(() =>
            {
                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrdersStatusRepository }); 
                var bo = (BOOrdersStatus)vm.BOOrdersStatus(OrdersStatusRepository);
                uow.Create(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                vm = new OrdersStatusVm(bo);
                return true;
            });
            await result;
            return CreatedAtRoute("DefaultApi", new { id = vm.İd }, vm);
        }

        // DELETE: api/OrdersStatus/5
        [ResponseType(typeof(OrdersStatusVm))]
        public async Task<IHttpActionResult> DeleteOrdersStatus(string id)
        {
            var result = Task.Factory.StartNew(() =>
            {
                 IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrdersStatusRepository }); 
                var bo = new BOOrdersStatus();
                bo.Repository = OrdersStatusRepository;
                bo.Init(id);
                
                uow.Delete(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return Ok(result.Result);
        }

        
        public IOrdersStatusRepository OrdersStatusRepository
        {
            get { return _iOrdersStatusRepository ?? (_iOrdersStatusRepository = RF.New().OrdersStatusRepository); }
            set { _iOrdersStatusRepository = value; }
        }
        

        protected override void Dispose(bool disposing)
        {
            if (disposing)
            {
            }
            base.Dispose(disposing);
        }
	}
}
