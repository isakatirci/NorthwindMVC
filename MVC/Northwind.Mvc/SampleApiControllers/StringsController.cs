/*************************************************************
** Class generated by CodeTrigger, Version 6.1.0.7
** This class was generated on 2.12.2018 01:22:24
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;
using System.Web.Http.Description;
using System.Threading.Tasks;
using Northwind.Mvc.SampleViewModels;
using Northwind.Business;
using Northwind.Business.Interfaces;
using Northwind.Business.Repository;
using Northwind.Business.Repository.Interfaces;

namespace Northwind.Mvc.SampleApiControllers
{
	public partial class StringsController : ApiController
	{
		protected IStringsRepository _iStringsRepository;

            // GET: api/Strings
        public async Task<IHttpActionResult> GetStrings(int page = 0, int pageSize = 5)
        {
            int totalCount = 0;

            IList<StringsVm> listVm = new List<StringsVm>();
            var result = Task.Factory.StartNew(() => {

                ICriteria criteria = new Criteria<BOStrings>(StringsRepository);
                totalCount = criteria.Count();

                IList<BOStrings> listBOs = criteria
                    .Add(new OrderBy("string_id", OrderBy.OrderDirection.Ascending))
                    .Skip(page * pageSize)
                    .Take(pageSize)
                    .List<BOStrings>();

                foreach (var bo in listBOs)
                    listVm.Add(new StringsVm(bo));
                return listVm;
            });
            await result;

            return Ok(new { Data = result.Result, Paging = new { Total = totalCount, Limit = pageSize, CurrentPage = page, PageCount = (int) Math.Ceiling((double)totalCount / pageSize) } });
        }

        // GET: api/Strings/5
        [ResponseType(typeof(StringsVm))]
        public async Task<IHttpActionResult> GetStrings(string id)
        {
            var result = Task.Factory.StartNew(() => {

                BOStrings bo = new Criteria<BOStrings>(StringsRepository)
                .Add(Expression.Eq("string_id", id))
                .SingleOrDefault<BOStrings>();
                return bo == null ? null : new StringsVm(bo);

            });
            await result;
            if (result.Result == null)
            {
                return NotFound();
            }

            return Ok(result.Result);
        }

        // PUT: api/Strings/5
        [ResponseType(typeof(void))]
        public async Task<IHttpActionResult> PutStrings(string id, StringsVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            if (id != vm.Stringİd)
            {
                return BadRequest();
            }

            var result = Task.Factory.StartNew(() => {

                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { StringsRepository }); 
                var bo = (BOStrings)vm.BOStrings(StringsRepository);
                uow.Update(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return StatusCode(HttpStatusCode.NoContent);
        }

        // POST: api/Strings
        [ResponseType(typeof(StringsVm))]
        public async Task<IHttpActionResult> PostStrings(StringsVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            var result = Task.Factory.StartNew(() =>
            {
                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { StringsRepository }); 
                var bo = (BOStrings)vm.BOStrings(StringsRepository);
                uow.Create(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                vm = new StringsVm(bo);
                return true;
            });
            await result;
            return CreatedAtRoute("DefaultApi", new { id = vm.Stringİd }, vm);
        }

        // DELETE: api/Strings/5
        [ResponseType(typeof(StringsVm))]
        public async Task<IHttpActionResult> DeleteStrings(string id)
        {
            var result = Task.Factory.StartNew(() =>
            {
                 IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { StringsRepository }); 
                var bo = new BOStrings();
                bo.Repository = StringsRepository;
                bo.Init(id);
                
                uow.Delete(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return Ok(result.Result);
        }

        
        public IStringsRepository StringsRepository
        {
            get { return _iStringsRepository ?? (_iStringsRepository = RF.New().StringsRepository); }
            set { _iStringsRepository = value; }
        }
        

        protected override void Dispose(bool disposing)
        {
            if (disposing)
            {
            }
            base.Dispose(disposing);
        }
	}
}
