/*************************************************************
** Class generated by CodeTrigger, Version 6.1.0.7
** This class was generated on 2.12.2018 01:22:24
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;
using System.Web.Http.Description;
using System.Threading.Tasks;
using Northwind.Mvc.SampleViewModels;
using Northwind.Business;
using Northwind.Business.Interfaces;
using Northwind.Business.Repository;
using Northwind.Business.Repository.Interfaces;

namespace Northwind.Mvc.SampleApiControllers
{
	public partial class OrderDetailsController : ApiController
	{
		protected IOrderDetailsRepository _iOrderDetailsRepository;

            // GET: api/OrderDetails
        public async Task<IHttpActionResult> GetOrderDetails(int page = 0, int pageSize = 5)
        {
            int totalCount = 0;

            IList<OrderDetailsVm> listVm = new List<OrderDetailsVm>();
            var result = Task.Factory.StartNew(() => {

                ICriteria criteria = new Criteria<BOOrderDetails>(OrderDetailsRepository);
                totalCount = criteria.Count();

                IList<BOOrderDetails> listBOs = criteria
                    .Add(new OrderBy("id", OrderBy.OrderDirection.Ascending))
                    .Skip(page * pageSize)
                    .Take(pageSize)
                    .List<BOOrderDetails>();

                foreach (var bo in listBOs)
                    listVm.Add(new OrderDetailsVm(bo));
                return listVm;
            });
            await result;

            return Ok(new { Data = result.Result, Paging = new { Total = totalCount, Limit = pageSize, CurrentPage = page, PageCount = (int) Math.Ceiling((double)totalCount / pageSize) } });
        }

        // GET: api/OrderDetails/5
        [ResponseType(typeof(OrderDetailsVm))]
        public async Task<IHttpActionResult> GetOrderDetails(string id)
        {
            var result = Task.Factory.StartNew(() => {

                BOOrderDetails bo = new Criteria<BOOrderDetails>(OrderDetailsRepository)
                .Add(Expression.Eq("id", id))
                .SingleOrDefault<BOOrderDetails>();
                return bo == null ? null : new OrderDetailsVm(bo);

            });
            await result;
            if (result.Result == null)
            {
                return NotFound();
            }

            return Ok(result.Result);
        }

        // PUT: api/OrderDetails/5
        [ResponseType(typeof(void))]
        public async Task<IHttpActionResult> PutOrderDetails(string id, OrderDetailsVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            if (id != vm.İd)
            {
                return BadRequest();
            }

            var result = Task.Factory.StartNew(() => {

                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrderDetailsRepository }); 
                var bo = (BOOrderDetails)vm.BOOrderDetails(OrderDetailsRepository);
                uow.Update(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return StatusCode(HttpStatusCode.NoContent);
        }

        // POST: api/OrderDetails
        [ResponseType(typeof(OrderDetailsVm))]
        public async Task<IHttpActionResult> PostOrderDetails(OrderDetailsVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            var result = Task.Factory.StartNew(() =>
            {
                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrderDetailsRepository }); 
                var bo = (BOOrderDetails)vm.BOOrderDetails(OrderDetailsRepository);
                uow.Create(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                vm = new OrderDetailsVm(bo);
                return true;
            });
            await result;
            return CreatedAtRoute("DefaultApi", new { id = vm.İd }, vm);
        }

        // DELETE: api/OrderDetails/5
        [ResponseType(typeof(OrderDetailsVm))]
        public async Task<IHttpActionResult> DeleteOrderDetails(string id)
        {
            var result = Task.Factory.StartNew(() =>
            {
                 IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrderDetailsRepository }); 
                var bo = new BOOrderDetails();
                bo.Repository = OrderDetailsRepository;
                bo.Init(id);
                
                uow.Delete(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return Ok(result.Result);
        }

        
        public IOrderDetailsRepository OrderDetailsRepository
        {
            get { return _iOrderDetailsRepository ?? (_iOrderDetailsRepository = RF.New().OrderDetailsRepository); }
            set { _iOrderDetailsRepository = value; }
        }
        

        protected override void Dispose(bool disposing)
        {
            if (disposing)
            {
            }
            base.Dispose(disposing);
        }
	}
}
