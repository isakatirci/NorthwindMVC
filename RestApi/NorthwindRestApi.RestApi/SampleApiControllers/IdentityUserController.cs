/*************************************************************
** Class generated by CodeTrigger, Version 6.1.0.7
** This class was generated on 1.12.2018 19:31:13
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;
using System.Web.Http.Description;
using System.Threading.Tasks;
using NorthwindRestApi.RestApi.SampleViewModels;
using NorthwindRestApi.Business;
using NorthwindRestApi.Business.Interfaces;
using NorthwindRestApi.Business.Repository;
using NorthwindRestApi.Business.Repository.Interfaces;

namespace NorthwindRestApi.RestApi.SampleApiControllers
{
	public partial class IdentityUserController : ApiController
	{
		protected IIdentityUserRepository _iIdentityUserRepository;

            // GET: api/IdentityUser
        public async Task<IHttpActionResult> GetIdentityUser(int page = 0, int pageSize = 5)
        {
            int totalCount = 0;

            IList<IdentityUserVm> listVm = new List<IdentityUserVm>();
            var result = Task.Factory.StartNew(() => {

                ICriteria criteria = new Criteria<BOIdentityUser>(IdentityUserRepository);
                totalCount = criteria.Count();

                IList<BOIdentityUser> listBOs = criteria
                    .Add(new OrderBy("UserId", OrderBy.OrderDirection.Ascending))
                    .Skip(page * pageSize)
                    .Take(pageSize)
                    .List<BOIdentityUser>();

                foreach (var bo in listBOs)
                    listVm.Add(new IdentityUserVm(bo));
                return listVm;
            });
            await result;

            return Ok(new { Data = result.Result, Paging = new { Total = totalCount, Limit = pageSize, CurrentPage = page, PageCount = (int) Math.Ceiling((double)totalCount / pageSize) } });
        }

        // GET: api/IdentityUser/5
        [ResponseType(typeof(IdentityUserVm))]
        public async Task<IHttpActionResult> GetIdentityUser(Int32 id)
        {
            var result = Task.Factory.StartNew(() => {

                BOIdentityUser bo = new Criteria<BOIdentityUser>(IdentityUserRepository)
                .Add(Expression.Eq("UserId", id))
                .SingleOrDefault<BOIdentityUser>();
                return bo == null ? null : new IdentityUserVm(bo);

            });
            await result;
            if (result.Result == null)
            {
                return NotFound();
            }

            return Ok(result.Result);
        }

        // PUT: api/IdentityUser/5
        [ResponseType(typeof(void))]
        public async Task<IHttpActionResult> PutIdentityUser(Int32 id, IdentityUserVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            if (id != vm.UserId)
            {
                return BadRequest();
            }

            var result = Task.Factory.StartNew(() => {

                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { IdentityUserRepository }); 
                var bo = (BOIdentityUser)vm.BOIdentityUser(IdentityUserRepository);
                uow.Update(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return StatusCode(HttpStatusCode.NoContent);
        }

        // POST: api/IdentityUser
        [ResponseType(typeof(IdentityUserVm))]
        public async Task<IHttpActionResult> PostIdentityUser(IdentityUserVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            var result = Task.Factory.StartNew(() =>
            {
                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { IdentityUserRepository }); 
                var bo = (BOIdentityUser)vm.BOIdentityUser(IdentityUserRepository);
                uow.Create(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                vm = new IdentityUserVm(bo);
                return true;
            });
            await result;
            return CreatedAtRoute("DefaultApi", new { id = vm.UserId }, vm);
        }

        // DELETE: api/IdentityUser/5
        [ResponseType(typeof(IdentityUserVm))]
        public async Task<IHttpActionResult> DeleteIdentityUser(Int32 id)
        {
            var result = Task.Factory.StartNew(() =>
            {
                 IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { IdentityUserRepository }); 
                var bo = new BOIdentityUser();
                bo.Repository = IdentityUserRepository;
                bo.Init(id);
                
                uow.Delete(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return Ok(result.Result);
        }

        
        public IIdentityUserRepository IdentityUserRepository
        {
            get { return _iIdentityUserRepository ?? (_iIdentityUserRepository = RF.New().IdentityUserRepository); }
            set { _iIdentityUserRepository = value; }
        }
        

        protected override void Dispose(bool disposing)
        {
            if (disposing)
            {
            }
            base.Dispose(disposing);
        }
	}
}
