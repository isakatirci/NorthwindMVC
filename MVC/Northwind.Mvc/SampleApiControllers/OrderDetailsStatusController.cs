/*************************************************************
** Class generated by CodeTrigger, Version 6.1.0.7
** This class was generated on 2.12.2018 01:22:24
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;
using System.Web.Http.Description;
using System.Threading.Tasks;
using Northwind.Mvc.SampleViewModels;
using Northwind.Business;
using Northwind.Business.Interfaces;
using Northwind.Business.Repository;
using Northwind.Business.Repository.Interfaces;

namespace Northwind.Mvc.SampleApiControllers
{
	public partial class OrderDetailsStatusController : ApiController
	{
		protected IOrderDetailsStatusRepository _iOrderDetailsStatusRepository;

            // GET: api/OrderDetailsStatus
        public async Task<IHttpActionResult> GetOrderDetailsStatus(int page = 0, int pageSize = 5)
        {
            int totalCount = 0;

            IList<OrderDetailsStatusVm> listVm = new List<OrderDetailsStatusVm>();
            var result = Task.Factory.StartNew(() => {

                ICriteria criteria = new Criteria<BOOrderDetailsStatus>(OrderDetailsStatusRepository);
                totalCount = criteria.Count();

                IList<BOOrderDetailsStatus> listBOs = criteria
                    .Add(new OrderBy("id", OrderBy.OrderDirection.Ascending))
                    .Skip(page * pageSize)
                    .Take(pageSize)
                    .List<BOOrderDetailsStatus>();

                foreach (var bo in listBOs)
                    listVm.Add(new OrderDetailsStatusVm(bo));
                return listVm;
            });
            await result;

            return Ok(new { Data = result.Result, Paging = new { Total = totalCount, Limit = pageSize, CurrentPage = page, PageCount = (int) Math.Ceiling((double)totalCount / pageSize) } });
        }

        // GET: api/OrderDetailsStatus/5
        [ResponseType(typeof(OrderDetailsStatusVm))]
        public async Task<IHttpActionResult> GetOrderDetailsStatus(string id)
        {
            var result = Task.Factory.StartNew(() => {

                BOOrderDetailsStatus bo = new Criteria<BOOrderDetailsStatus>(OrderDetailsStatusRepository)
                .Add(Expression.Eq("id", id))
                .SingleOrDefault<BOOrderDetailsStatus>();
                return bo == null ? null : new OrderDetailsStatusVm(bo);

            });
            await result;
            if (result.Result == null)
            {
                return NotFound();
            }

            return Ok(result.Result);
        }

        // PUT: api/OrderDetailsStatus/5
        [ResponseType(typeof(void))]
        public async Task<IHttpActionResult> PutOrderDetailsStatus(string id, OrderDetailsStatusVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            if (id != vm.İd)
            {
                return BadRequest();
            }

            var result = Task.Factory.StartNew(() => {

                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrderDetailsStatusRepository }); 
                var bo = (BOOrderDetailsStatus)vm.BOOrderDetailsStatus(OrderDetailsStatusRepository);
                uow.Update(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return StatusCode(HttpStatusCode.NoContent);
        }

        // POST: api/OrderDetailsStatus
        [ResponseType(typeof(OrderDetailsStatusVm))]
        public async Task<IHttpActionResult> PostOrderDetailsStatus(OrderDetailsStatusVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            var result = Task.Factory.StartNew(() =>
            {
                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrderDetailsStatusRepository }); 
                var bo = (BOOrderDetailsStatus)vm.BOOrderDetailsStatus(OrderDetailsStatusRepository);
                uow.Create(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                vm = new OrderDetailsStatusVm(bo);
                return true;
            });
            await result;
            return CreatedAtRoute("DefaultApi", new { id = vm.İd }, vm);
        }

        // DELETE: api/OrderDetailsStatus/5
        [ResponseType(typeof(OrderDetailsStatusVm))]
        public async Task<IHttpActionResult> DeleteOrderDetailsStatus(string id)
        {
            var result = Task.Factory.StartNew(() =>
            {
                 IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { OrderDetailsStatusRepository }); 
                var bo = new BOOrderDetailsStatus();
                bo.Repository = OrderDetailsStatusRepository;
                bo.Init(id);
                
                uow.Delete(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return Ok(result.Result);
        }

        
        public IOrderDetailsStatusRepository OrderDetailsStatusRepository
        {
            get { return _iOrderDetailsStatusRepository ?? (_iOrderDetailsStatusRepository = RF.New().OrderDetailsStatusRepository); }
            set { _iOrderDetailsStatusRepository = value; }
        }
        

        protected override void Dispose(bool disposing)
        {
            if (disposing)
            {
            }
            base.Dispose(disposing);
        }
	}
}
