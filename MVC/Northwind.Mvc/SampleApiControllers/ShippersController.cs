/*************************************************************
** Class generated by CodeTrigger, Version 6.1.0.7
** This class was generated on 2.12.2018 01:22:24
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;
using System.Web.Http.Description;
using System.Threading.Tasks;
using Northwind.Mvc.SampleViewModels;
using Northwind.Business;
using Northwind.Business.Interfaces;
using Northwind.Business.Repository;
using Northwind.Business.Repository.Interfaces;

namespace Northwind.Mvc.SampleApiControllers
{
	public partial class ShippersController : ApiController
	{
		protected IShippersRepository _iShippersRepository;

            // GET: api/Shippers
        public async Task<IHttpActionResult> GetShippers(int page = 0, int pageSize = 5)
        {
            int totalCount = 0;

            IList<ShippersVm> listVm = new List<ShippersVm>();
            var result = Task.Factory.StartNew(() => {

                ICriteria criteria = new Criteria<BOShippers>(ShippersRepository);
                totalCount = criteria.Count();

                IList<BOShippers> listBOs = criteria
                    .Add(new OrderBy("id", OrderBy.OrderDirection.Ascending))
                    .Skip(page * pageSize)
                    .Take(pageSize)
                    .List<BOShippers>();

                foreach (var bo in listBOs)
                    listVm.Add(new ShippersVm(bo));
                return listVm;
            });
            await result;

            return Ok(new { Data = result.Result, Paging = new { Total = totalCount, Limit = pageSize, CurrentPage = page, PageCount = (int) Math.Ceiling((double)totalCount / pageSize) } });
        }

        // GET: api/Shippers/5
        [ResponseType(typeof(ShippersVm))]
        public async Task<IHttpActionResult> GetShippers(string id)
        {
            var result = Task.Factory.StartNew(() => {

                BOShippers bo = new Criteria<BOShippers>(ShippersRepository)
                .Add(Expression.Eq("id", id))
                .SingleOrDefault<BOShippers>();
                return bo == null ? null : new ShippersVm(bo);

            });
            await result;
            if (result.Result == null)
            {
                return NotFound();
            }

            return Ok(result.Result);
        }

        // PUT: api/Shippers/5
        [ResponseType(typeof(void))]
        public async Task<IHttpActionResult> PutShippers(string id, ShippersVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            if (id != vm.İd)
            {
                return BadRequest();
            }

            var result = Task.Factory.StartNew(() => {

                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { ShippersRepository }); 
                var bo = (BOShippers)vm.BOShippers(ShippersRepository);
                uow.Update(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return StatusCode(HttpStatusCode.NoContent);
        }

        // POST: api/Shippers
        [ResponseType(typeof(ShippersVm))]
        public async Task<IHttpActionResult> PostShippers(ShippersVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            var result = Task.Factory.StartNew(() =>
            {
                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { ShippersRepository }); 
                var bo = (BOShippers)vm.BOShippers(ShippersRepository);
                uow.Create(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                vm = new ShippersVm(bo);
                return true;
            });
            await result;
            return CreatedAtRoute("DefaultApi", new { id = vm.İd }, vm);
        }

        // DELETE: api/Shippers/5
        [ResponseType(typeof(ShippersVm))]
        public async Task<IHttpActionResult> DeleteShippers(string id)
        {
            var result = Task.Factory.StartNew(() =>
            {
                 IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { ShippersRepository }); 
                var bo = new BOShippers();
                bo.Repository = ShippersRepository;
                bo.Init(id);
                
                uow.Delete(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return Ok(result.Result);
        }

        
        public IShippersRepository ShippersRepository
        {
            get { return _iShippersRepository ?? (_iShippersRepository = RF.New().ShippersRepository); }
            set { _iShippersRepository = value; }
        }
        

        protected override void Dispose(bool disposing)
        {
            if (disposing)
            {
            }
            base.Dispose(disposing);
        }
	}
}
