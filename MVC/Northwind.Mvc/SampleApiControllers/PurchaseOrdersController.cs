/*************************************************************
** Class generated by CodeTrigger, Version 6.1.0.7
** This class was generated on 2.12.2018 01:22:24
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web.Http;
using System.Web.Http.Description;
using System.Threading.Tasks;
using Northwind.Mvc.SampleViewModels;
using Northwind.Business;
using Northwind.Business.Interfaces;
using Northwind.Business.Repository;
using Northwind.Business.Repository.Interfaces;

namespace Northwind.Mvc.SampleApiControllers
{
	public partial class PurchaseOrdersController : ApiController
	{
		protected IPurchaseOrdersRepository _iPurchaseOrdersRepository;

            // GET: api/PurchaseOrders
        public async Task<IHttpActionResult> GetPurchaseOrders(int page = 0, int pageSize = 5)
        {
            int totalCount = 0;

            IList<PurchaseOrdersVm> listVm = new List<PurchaseOrdersVm>();
            var result = Task.Factory.StartNew(() => {

                ICriteria criteria = new Criteria<BOPurchaseOrders>(PurchaseOrdersRepository);
                totalCount = criteria.Count();

                IList<BOPurchaseOrders> listBOs = criteria
                    .Add(new OrderBy("id", OrderBy.OrderDirection.Ascending))
                    .Skip(page * pageSize)
                    .Take(pageSize)
                    .List<BOPurchaseOrders>();

                foreach (var bo in listBOs)
                    listVm.Add(new PurchaseOrdersVm(bo));
                return listVm;
            });
            await result;

            return Ok(new { Data = result.Result, Paging = new { Total = totalCount, Limit = pageSize, CurrentPage = page, PageCount = (int) Math.Ceiling((double)totalCount / pageSize) } });
        }

        // GET: api/PurchaseOrders/5
        [ResponseType(typeof(PurchaseOrdersVm))]
        public async Task<IHttpActionResult> GetPurchaseOrders(string id)
        {
            var result = Task.Factory.StartNew(() => {

                BOPurchaseOrders bo = new Criteria<BOPurchaseOrders>(PurchaseOrdersRepository)
                .Add(Expression.Eq("id", id))
                .SingleOrDefault<BOPurchaseOrders>();
                return bo == null ? null : new PurchaseOrdersVm(bo);

            });
            await result;
            if (result.Result == null)
            {
                return NotFound();
            }

            return Ok(result.Result);
        }

        // PUT: api/PurchaseOrders/5
        [ResponseType(typeof(void))]
        public async Task<IHttpActionResult> PutPurchaseOrders(string id, PurchaseOrdersVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            if (id != vm.İd)
            {
                return BadRequest();
            }

            var result = Task.Factory.StartNew(() => {

                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { PurchaseOrdersRepository }); 
                var bo = (BOPurchaseOrders)vm.BOPurchaseOrders(PurchaseOrdersRepository);
                uow.Update(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return StatusCode(HttpStatusCode.NoContent);
        }

        // POST: api/PurchaseOrders
        [ResponseType(typeof(PurchaseOrdersVm))]
        public async Task<IHttpActionResult> PostPurchaseOrders(PurchaseOrdersVm vm)
        {
            if (!ModelState.IsValid)
            {
                return BadRequest(ModelState);
            }

            var result = Task.Factory.StartNew(() =>
            {
                IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { PurchaseOrdersRepository }); 
                var bo = (BOPurchaseOrders)vm.BOPurchaseOrders(PurchaseOrdersRepository);
                uow.Create(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                vm = new PurchaseOrdersVm(bo);
                return true;
            });
            await result;
            return CreatedAtRoute("DefaultApi", new { id = vm.İd }, vm);
        }

        // DELETE: api/PurchaseOrders/5
        [ResponseType(typeof(PurchaseOrdersVm))]
        public async Task<IHttpActionResult> DeletePurchaseOrders(string id)
        {
            var result = Task.Factory.StartNew(() =>
            {
                 IUnitOfWork uow = new UnitOfWorkImp(new IRepositoryConnection[] { PurchaseOrdersRepository }); 
                var bo = new BOPurchaseOrders();
                bo.Repository = PurchaseOrdersRepository;
                bo.Init(id);
                
                uow.Delete(bo);

                string err;
                if (!uow.Commit(out err))
                {
                    var resp = new HttpResponseMessage(HttpStatusCode.BadRequest) { Content = new StringContent(err) };
                    throw new HttpResponseException(resp);
                }
                return true;
            });
            await result;
            if (!result.Result)
                return NotFound();

            return Ok(result.Result);
        }

        
        public IPurchaseOrdersRepository PurchaseOrdersRepository
        {
            get { return _iPurchaseOrdersRepository ?? (_iPurchaseOrdersRepository = RF.New().PurchaseOrdersRepository); }
            set { _iPurchaseOrdersRepository = value; }
        }
        

        protected override void Dispose(bool disposing)
        {
            if (disposing)
            {
            }
            base.Dispose(disposing);
        }
	}
}
